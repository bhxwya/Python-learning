# BEFORE:
# This file was empty.


# AFTER:
# Import functions from the modules so they can be accessed
# directly through the my_utils package.

from .calculator import add, subtract, multiply
from .string_utils import uppercase, lowercase, reverse